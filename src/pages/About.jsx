 import React from "react";

const About = () => (
  <section className="about card-shadow">
    <h2>About This App</h2>
    <p>
      A minimal, responsive ToDo application built with React. Uses an external
      CSS file (src/styles/app.css) and localStorage to persist tasks. Designed
      to be mobile-first and easy to style.
    </p>
  </section>
);

export default About;
